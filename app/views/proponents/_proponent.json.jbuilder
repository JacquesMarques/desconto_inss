json.extract! proponent, :id, :name, :document, :birthday, :address_id, :personal_phone, :references_phone, :salary, :inss_discount, :created_at, :updated_at
json.url proponent_url(proponent, format: :json)
